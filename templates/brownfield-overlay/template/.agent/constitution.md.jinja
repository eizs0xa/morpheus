# {{ project_name }} Constitution

> This is project law. It governs how contributors (human and agent) design, build,
> test, and ship this project. It is authored by the steward of record and amended via
> PR + ADR. Parent law: the Morpheus Platform Constitution — this document inherits
> from it and may be stricter, never laxer.
>
> **This constitution was installed via the Morpheus brownfield overlay.** The overlay
> did not modify any source directories. See `.agent/BROWNFIELD_NOTES.md` for the list
> of files that were backed up and where originals live.

**Project:** {{ project_name }}
**Steward of record:** <fill in a named human>
**Default profile for this project:** {{ profile }}
**Primary stacks:** {{ stacks if stacks else "(none declared)" }}
**Workspace:** {{ workspace }}
**Project-management tool:** {{ pm if pm else "(none)" }}
**Git provider:** {{ git }}

---

## 1. Purpose

Three to five sentences describing what this project is and why it exists. Write this
as if a new contributor is reading it on day one.

## 2. Scope

### In scope
- <bullet>

### Out of scope
- <bullet>

Anything not listed as in-scope requires a constitutional amendment to add, or a
decision to carry it in a different repo/project.

## 3. Non-negotiables

Rules this project never breaks. Each clause is a single, testable statement.

1. <clause>
2. <clause>
3. <clause>

The platform stop-lines (from the Morpheus Platform Constitution) apply here verbatim
and cannot be weakened:

- **No sixth profile.**
- **No per-profile constitutions or gates.**
- **Profiles are ergonomic, not runtime permissions.**
- **Max 5 init questions.**
- **No profile × stack × project-type matrices.**
- **No PM-tool validation expansion beyond initiative existence.**
- **No project-type branching inside `constitution-author`.**

## 4. Architectural boundaries

Describe the modules, services, or layers that make up this project and the rules
that govern cross-boundary communication. Attach a Mermaid diagram if it helps.

- <boundary>
- <boundary>

## 5. Coding standards reference

This constitution does not restate coding standards. It names the binding references:

- Linting / formatting: `<path to tooling config>`
- Per-stack instructions: `<path(s) to instruction files>`
- Commit conventions: <conventional commits / other>
- Review style: <trunk-based / feature branches / release train>

## 6. Testing standards reference

- Unit test target: `<path and command>`
- Integration test target: `<path and command>`
- Holdout test target (post-merge): `<path and command>`
- Coverage threshold: <number or "see stack instructions">

## 7. Security standards

Minimum posture:

- **Authentication / authorization:** <policy>
- **Secrets:** <where they live, how they rotate, what is forbidden in repo>
- **Logging:** <what must be logged; what must never be logged>
- **Data compliance regime(s):** <HIPAA / SOC2 / GDPR / none — be specific>
- **Dependency policy:** <SCA cadence, SBOM requirements>
- **Incident response:** <who is paged and how>

## 8. Amendment process

1. Open a PR that edits this constitution and adds an ADR under `docs/decisions/`.
2. Steward of record reviews the PR for consistency with platform law and project
   scope.
3. At least one additional maintainer approves.
4. Merge and tag a new project version — constitutions are versioned with the project.

## 9. Brownfield provenance

This repository received the Morpheus overlay on a pre-existing codebase. The
following artifacts are overlay-managed and MUST NOT be edited by hand:

- `.agent/` (full subtree)
- `platform-manifest.json`
- `.github/workflows/morpheus-*.yml`

The following are overlay-managed but may be hand-edited once the merge with any
pre-existing content is complete:

- `CLAUDE.md`
- `AGENTS.md`
- `.github/copilot-instructions.md`

Backups of anything the overlay replaced are in `*.pre-morpheus.bak` files at the
repo root and under `.github/`. Do not delete these without steward approval.

---

*Generated by the Morpheus brownfield overlay.*
