{
  "name": "brownfield-node-fixture",
  "version": "0.0.1",
  "description": "Minimal existing node+ts project — used as a brownfield-overlay test fixture. Do not modify at runtime.",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "echo 'no tests yet'"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
