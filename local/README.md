# Local

Local is for private experiments, scratch work, local evidence, and machine-specific notes.

This folder is ignored except for this README.

Use `local/` before promoting anything to `incubator/`.