# {{ project_name }} Morpheus Workspace

Status: setup generated. Review this file before running initiation.

## Workspace Folders

- `morpheus`
- Add product repos from `project.config.json`.

## Enabled Modules

- `workspace-companion`

## Next Action

Run Morpheus initiation from agent chat when ready.

## Local Config

Copy `.env.example` to `.env` and fill secrets locally. Never paste secrets into chat.

## Existing Agent Assets

Discovery will record existing `agent.md`, `AGENTS.md`, Copilot instructions, and `SKILL.md` files in product repos.