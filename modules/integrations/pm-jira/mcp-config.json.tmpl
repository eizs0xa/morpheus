{
  "mcpServers": {
    "jira": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-jira"],
      "env": {
        "JIRA_SITE_URL": "{{ jira_site_url }}",
        "JIRA_PROJECT_KEY": "{{ jira_project_key }}"
      }
    }
  }
}
